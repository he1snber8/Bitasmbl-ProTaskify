// client/src/components/Login.js\nimport React,{useState} from 'react';\nimport api from '../services/api';\nfunction Login(){const [email,setEmail]=useState('');const [pw,setPw]=useState('');const login=()=>api.post('/auth/login',{email,pw}).then(r=>localStorage.setItem('token',r.data.token));return <div><input value={email} onChange={e=>setEmail(e.target.value)}/><input type='password' value={pw} onChange={e=>setPw(e.target.value)}/><button onClick={login}>Login</button></div>;}\nexport default Login;